# Paper project instructions

Use the local `paper_heuristic_system` skill for manuscript revision.

Project rules:

- Keep all paper-specific state under `paper_hs/`.
- Do not import memory from other papers.
- Run citation verification before editing related work, contribution bullets, novelty claims, or BibTeX.
- Record every non-trivial patch under `paper_hs/runs/`.
